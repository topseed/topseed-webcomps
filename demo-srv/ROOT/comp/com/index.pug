extends  ../../_part/_baseShell

block head
	title CompCom
	include ../../_part/_header

block main
	.mui-container-fluid

		p Show how event in c-1 affects c-2.

		c-1

		c-2

	script.
		TS.onAppReady(UIinit)
		
		let _blx
		
		function UIinit() {
			console.log('pg')

			_blx = new BLX(null) // can / should have folder local class

			TW.loadComp($('#comps'),'/_webComp/C1.html', UIComp1Init)
			TW.loadComp($('#comps'),'/_webComp/C2.html', UIComp2Init)
		}

		function UIComp1Init() {
			var xx = TW.cRegOrGet('c-1', C1El)
			xx.init(_blx) // talk to it
		}

		function UIComp2Init() {
			var xx = TW.cRegOrGet('c-2', C2El)
			xx.init(_blx) // talk to it
		}



block footer
	include ../../_part/_footer


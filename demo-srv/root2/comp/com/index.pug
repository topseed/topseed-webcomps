extends  ../../_part/_baseShell

block head
	title CompCom
	include ../../_part/_header

block main
	.mui-container-fluid

		p Show how event in c1-el affects c2-el.

		c1-el

		c2-el

	script.
		TS.onAppReady(loadComps)
		
		function UIinit() {
			console.log('pg')

			var _blx = new BLX(null) // can / should have folder local class
			
			var c = document.querySelector('c1-el')
			c.init(_blx) // registerComp and talk to it

			var c = document.querySelector('c2-el')
			c.init(_blx) // registerComp talk to it
		}

		function loadComps() {
			//#comps is a div in _footer.pug
			Promise.all([
				TW.loadComp('/_webComp/C1.html', $('#comps')
				TW.loadComp('/_webComp/C2.html', $('#comps')
			]).then(UIinit)
		}

block footer
	include ../../_part/_footer

